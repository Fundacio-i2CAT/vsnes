# Minimal olsrd configuration for the VSNES mesh test.
# On the flat /24 bridge every node hears every other node, so olsrd should
# discover all peers as 1-hop neighbours. Inspect with:
#   echo "/neigh" | nc 127.0.0.1 2006
#   echo "/topo"  | nc 127.0.0.1 2006

DebugLevel  0
IpVersion   4

# Plugin filename is versioned by the build; routing-ctl auto-detects the
# actual olsrd_txtinfo.so.* at start time and rewrites this LoadPlugin line.
LoadPlugin "olsrd_txtinfo.so.1.1"
{
    PlParam "accept" "0.0.0.0"
}

Interface "eth0"
{
}
