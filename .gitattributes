# Shell scripts must keep LF endings — they run in Linux/WSL containers, where
# CRLF would break the shebang and `read`/`case` parsing. (core.autocrlf=true
# would otherwise rewrite them to CRLF on checkout.)
*.sh text eol=lf
